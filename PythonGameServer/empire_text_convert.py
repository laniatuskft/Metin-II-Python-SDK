## Laniatus Games Studio Inc. | NOTE: Anonymous namespaces are not defined:
#namespace
class STextConvertTable:

    def __init__(self):
        #instance fields found by # Laniatus Games Studio Inc. |:
        self.acUpper = str(['\0' for _ in range(26)])
        self.acLower = str(['\0' for _ in range(26)])
        self.aacHan = [[] for _ in range(5000)]
        self.aacJaum = [[] for _ in range(50)]
        self.aacMoum = [[] for _ in range(50)]
